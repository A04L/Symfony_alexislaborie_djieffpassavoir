{# templates/navigation.html.twig #}
<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Mon Site</title>
		<!-- Inclusion du fichier CSS -->
		<link rel="stylesheet" href="{{ asset('styles/app.css') }}">
	</head>
	<nav>
		<ul>
			<li class="nav-item">
				<a href="{{ path('index') }}">Home</a>
			</li>
			<li class="nav-item">
				<a class="test" href="{{path('blog_index')}}">Blog</a>
			</li>
			<li class="nav-item">
				<a class="test" href="{{path('app_login')}}">Connexion</a>
			</li>
			<li class="nav-item">
				<a class="test" href="{{path('app_register')}}">Inscription</a>
			</li>
		</ul>
	</nav>
</html>
